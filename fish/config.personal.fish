# FILE: ~/.config/fish/config.personal.fish
# Sourced by config.fish. Put your personal functions and variables here.

# -- ALIASES --
# alias my-server 'ssh user@my-server.com'
# alias aye 'cd ~/projects/my-project; and podman-compose up -d'

# -- ENVIRONMENT VARIABLES (set -Ux makes them universal and exported) --
# Be careful with committing secrets to version control!
# set -Ux OPENAI_API_KEY "sk-..."
# set -Ux GITHUB_TOKEN "ghp_..."

# -- FUNCTIONS --
# function backup_database
#     echo "Backing up my_database..."
#     pg_dump -U myuser -h localhost my_database > "$HOME/backups/db_(date +%F).sql"
# end
